diff --git a/app/Http/Controllers/Api/RiderController.php b/app/Http/Controllers/Api/RiderController.php
index 7b627e7..276b06b 100644
--- a/app/Http/Controllers/Api/RiderController.php
+++ b/app/Http/Controllers/Api/RiderController.php
@@ -78,8 +78,13 @@ public function register(Request $request)
                     'plate_no' => 'required',
                     'vehicle_make' => 'required',
                     'vehicle_model' => 'required', 
-                    // 'bank_name' => 'required',  
-                    // 'bank_no' => 'required',  
+                    // Now required — the app's Bank Information
+                    // section is no longer marked "(optional)" and
+                    // validates this client-side before allowing Next.
+                    // Enforced here too so a direct API call (bypassing
+                    // the app's own validation) can't skip it.
+                    'bank_name' => 'required',
+                    'bank_no' => 'required',
                     
                     'ic_front' => 'image|mimes:jpg,png|max:10240',
                     'ic_back' => 'image|mimes:jpg,png|max:10240',
