diff --git a/app/Filament/Resources/OrderResource/Pages/EditOrder.php b/app/Filament/Resources/OrderResource/Pages/EditOrder.php
index b84480b..56a4925 100644
--- a/app/Filament/Resources/OrderResource/Pages/EditOrder.php
+++ b/app/Filament/Resources/OrderResource/Pages/EditOrder.php
@@ -1276,6 +1276,42 @@ class="w-5 h-5 text-green-500" fill="none" viewBox="0 0 24 24"
                                             Placeholder::make('total')->label(false)->content('RM ' . number_format($this->record->grand_total, 2)),
                                         ]),
                                 ]),
+                            Section::make('Dry Cleaning Items')
+                                ->description('Itemized breakdown for this order\'s per-piece pricing — only shown for orders placed under the Dry Cleaning service category.')
+                                ->visible(fn () => !empty($this->record->booking?->items))
+                                ->schema([
+                                    Placeholder::make('dry_clean_items_breakdown')
+                                        ->label(false)
+                                        ->content(function () {
+                                            $booking = $this->record->booking;
+                                            $items = $booking?->items ?? [];
+                                            if (empty($items)) {
+                                                return new \Illuminate\Support\HtmlString('');
+                                            }
+                                            $rows = '';
+                                            foreach ($items as $item) {
+                                                $rows .= '<tr>'
+                                                    . '<td style="padding:4px 12px 4px 0;">' . e($item['name'] ?? '-') . '</td>'
+                                                    . '<td style="padding:4px 12px;text-align:center;">' . e($item['quantity'] ?? 0) . '</td>'
+                                                    . '<td style="padding:4px 12px;text-align:right;">RM' . number_format((float) ($item['unit_price'] ?? 0), 2) . '</td>'
+                                                    . '<td style="padding:4px 0 4px 12px;text-align:right;">RM' . number_format((float) ($item['subtotal'] ?? 0), 2) . '</td>'
+                                                    . '</tr>';
+                                            }
+                                            $discountPercent = $booking->dry_clean_discount_percent ?? 0;
+                                            $discountNote = $discountPercent > 0
+                                                ? '<p style="font-size:12px;color:#4b5563;margin-top:6px;">Subscriber discount applied: ' . number_format($discountPercent, 2) . '% (already reflected in Washing (RM) on the order list — this table shows pre-discount item pricing).</p>'
+                                                : '';
+                                            $html = '<table style="border-collapse:collapse;font-size:13px;">'
+                                                . '<thead><tr style="border-bottom:1px solid #e5e7eb;font-weight:600;">'
+                                                . '<td style="padding:4px 12px 4px 0;">Item</td>'
+                                                . '<td style="padding:4px 12px;text-align:center;">Qty</td>'
+                                                . '<td style="padding:4px 12px;text-align:right;">Unit Price</td>'
+                                                . '<td style="padding:4px 0 4px 12px;text-align:right;">Subtotal</td>'
+                                                . '</tr></thead><tbody>' . $rows . '</tbody></table>'
+                                                . $discountNote;
+                                            return new \Illuminate\Support\HtmlString($html);
+                                        }),
+                                ]),
                             Section::make('Laundry Bag Pickup Info')
                                 ->schema([
                                     Grid::make(2)
